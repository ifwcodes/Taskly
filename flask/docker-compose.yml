8version: '3.8'

services:
  task-tracker:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./static:/app/static
      - ./templates:/app/templates
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    command: python app.py --host 0.0.0.0
